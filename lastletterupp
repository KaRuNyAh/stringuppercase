


#include<stdio.h>
#include<string.h>
int main(){
//in a sentence make last letter to uppercase
char s[30];
gets(s);
int n=strlen(s);
for(int i=0;s[i]!='\0';i++){
if(s[i]==32)
{
    s[i-1]=s[i-1]-32;
}
}
s[n-1]=s[n-1]-32;
printf("%s",s);
printf("\n %d",n);
}
